# vtk DataFile Version 2.0
Unstructured Grid Example
ASCII
DATASET UNSTRUCTURED_GRID

POINTS {{nodesNumber}} float
{% for node in nodes %}{{node.x}} {{node.y}} 0.0
{% endfor %}
CELLS {{elementsNumber}} {{sumOfElementsData}}
{% for i in range(elementsNumber) %}{{elementNodesNumber[i]}}{% for id in elements[i].IDs %} {{id - 1}}{% endfor %}
{% endfor %}
CELL_TYPES {{elementsNumber}}
{% for element in elements %}9
{% endfor %}
POINT_DATA {{nodesNumber}}
SCALARS Temp float 1
LOOKUP_TABLE default
{% for t in temperatures %}{{t[0]}}
{% endfor %}